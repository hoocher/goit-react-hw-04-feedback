import styled from 'styled-components';

export const OptButton = styled('button')(() => {
  return {
    minWidth: '120px',
    padding: '5px',
    textAlign: 'center',
    fontSize: '20px',
    marginLeft: '10px',
  };
});
